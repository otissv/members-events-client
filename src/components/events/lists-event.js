'use strict';

import { propTypes } from '../../props';


const Events = (props) => (
  <div>
  <h1>Events Page</h1>
  <p>User is logged in</p>
  </div>
);

Events.propTypes = propTypes;

export default Events;
